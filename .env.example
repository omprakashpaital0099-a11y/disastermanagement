# Optional: Mapbox public access token for Mapbox light/dark basemaps.
# Without this value the dashboard uses keyless OpenStreetMap/CartoDB tiles.
MAPBOX_TOKEN=

# Existing backend URL, if it is not the local default.
PRITHVINET_API_URL=http://localhost:8000/api/v1